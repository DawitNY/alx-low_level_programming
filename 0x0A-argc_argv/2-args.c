#include <stdio.h>

/**
 * main - prints args received 
 * argc: count
 * @argv: array
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = =; i < argc; i ++)
		printf("%s\n", argv[i]);
	return (0);
}
